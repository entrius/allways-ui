# API base URL
VITE_REACT_APP_BASE_URL=http://localhost:9081

# Override the documentation link (falls back to hostname-based default)
# VITE_DOCS_URL=https://docs.all-ways.io/

# Dev-only: override the target the `/api` proxy points at in `vite dev`
# VITE_DEV_API_TARGET=https://test-api.all-ways.io
